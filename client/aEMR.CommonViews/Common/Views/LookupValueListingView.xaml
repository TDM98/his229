<UserControl x:Class="aEMR.Common.Views.LookupValueListingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:customcontrols="clr-namespace:aEMR.Controls;assembly=aEMR.Controls"
             d:DesignHeight="300" d:DesignWidth="400"
             mc:Ignorable="d">

  <Grid x:Name="LayoutRoot">
        <!--KMx: Để style cho đồng nhất (11/07/2014 11:00)-->
        <!--<customcontrols:AxComboBox Height="22"
                               SelectedItemEx="{Binding SelectedItem, Mode=TwoWay}" DisplayMemberPath="ObjectValue" SelectedValuePath="LookupID"
                               ItemsSource="{Binding LookupList,Mode=OneWay}" Style="{StaticResource ComboBoxStyle}"/>-->
        <customcontrols:AxComboBox Height="22"
                               SelectedItemEx="{Binding SelectedItem, Mode=TwoWay}" DisplayMemberPath="ObjectValue" SelectedValuePath="LookupID"
                               ItemsSource="{Binding LookupList,Mode=OneWay}" Style="{StaticResource MainFrame.ComboBox}"/>

    </Grid>
</UserControl>